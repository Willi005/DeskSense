node_modules
dist
dist-electron
.env
.env.local
*.log
.DS_Store

# Empaquetado (electron-builder) e iconos generados (se regeneran desde icon.svg)
release
build/icon.ico
build/icon.png

# Espacio de trabajo temporal de ejecución de planes (registro, informes, diffs)
.superpowers

# Credenciales reales del firmware (WiFi y token del dispositivo).
# La plantilla versionada es esp32/secrets.example.h.
esp32/secrets.h

# Cache de compilacion y toolchain de PlatformIO (cientos de MB).
esp32/.pio/
